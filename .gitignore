# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Nix
/result
/result-*
.direnv/

# GSettings compiled schema (generated by glib-compile-schemas)
/data/gschemas.compiled

# Editor
*.swp
.idea/

# Telegram API credentials are passed as env vars at build time (see
# secrets.nix.example). No secret files are ever committed.
